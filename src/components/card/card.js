
import React from 'react';
// import PropTypes from 'prop-types';

const C_CARD = ( props ) => {

    const {} = props;

    return (
        <>
            
        </>
    );

};


C_CARD.propTypes = {
};

C_CARD.defaultProps = {
};

export {
    C_CARD,
};
